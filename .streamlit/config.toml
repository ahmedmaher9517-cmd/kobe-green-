# Streamlit Cloud — لا تغيّر address/port (المنصة تضبطها تلقائياً)
[server]
headless = true
enableCORS = false
enableXsrfProtection = false

[browser]
gatherUsageStats = false

[runner]
fastReruns = true

[client]
showErrorDetails = true
